# Signed Nexus Pro APK

Place the release-signed `nexus-pro.apk` here for direct download at `/releases/nexus-pro.apk`.

Update `public/android-release.json` (version, sha256, sizeBytes) when shipping a new build.
