# Phase B: authoritative compiler + Next.js type validation (see next.config.mjs).
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      # v5+ uses Node 24 for the action runtime (see GitHub deprecation of Node 20 on runners).
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck + production build (Next enforces types)
        env:
          NEXT_TELEMETRY_DISABLED: "1"
        run: npm run verify:ci
