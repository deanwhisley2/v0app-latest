/** @deprecated Use ConnectivityStrip on dashboard; global fixed banner removed to avoid header stack clash. */
export { ConnectivityStrip as ConnectivityBanner } from "@/components/mobile/connectivity-strip"
