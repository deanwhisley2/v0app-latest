# After git pull on VPS — append to /opt/nexus-pro/.env.local (do not commit real keys):

# Brevo SMTP — https://app.brevo.com/settings/keys/smtp (relay key xsmtpsib…)
echo "BREVO_SMTP_HOST=smtp-relay.brevo.com" >> .env.local
echo "BREVO_SMTP_PORT=587" >> .env.local
echo "BREVO_SMTP_USER=your-brevo-login@email.com" >> .env.local
echo "BREVO_SMTP_PASSWORD=PASTE_xsmtpsib_SMTP_KEY_HERE" >> .env.local
echo "BREVO_SENDER_EMAIL=security@nexuspro.it.com" >> .env.local
echo "BREVO_SENDER_NAME=Nexus Pro Security" >> .env.local
echo "TRANSACTIONAL_REPLY_TO_EMAIL=support@nexuspro.it.com" >> .env.local

# Remove legacy Cyberpersons vars if present:
# sed -i '/^CYBERPERSONS_/d' .env.local

# Docs: docs/MAGIC_LINK_AUTH_SETUP.md
# Health: optional_services.brevo_smtp_configured on GET /api/health/launch
# Check: npm run brevo:smtp-check
